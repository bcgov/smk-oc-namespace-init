apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smk-namespace-init.fullname" . }}-test-connection"
  labels:
    {{- include "smk-namespace-init.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smk-namespace-init.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
